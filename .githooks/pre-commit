#!/usr/bin/env sh
set -e

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks not found on PATH; commit blocked. Install gitleaks and retry." >&2
  exit 1
fi

gitleaks git --staged --redact .
